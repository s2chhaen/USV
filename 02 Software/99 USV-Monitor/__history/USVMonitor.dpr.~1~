program USVMonitor;

uses
  Forms,
  BCommander in 'BCommander.pas' {Form1};

{$R *.RES}

begin
  Application.Initialize;
  Application.Title := 'USV-Slave Monitor';
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
